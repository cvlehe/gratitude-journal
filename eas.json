{
  "cli": {
    "version": ">= 16.28.0",
    "appVersionSource": "remote"
  },
  "build": {
    "development": {
      "developmentClient": true,
      "distribution": "internal",
      "hooks": {
        "pre-install": "node -e \"console.log('NPM_TOKEN present:', Boolean(process.env.NPM_TOKEN))\" && ls -la && (test -f .npmrc && echo '.npmrc found' || echo '.npmrc missing')"
      }
    },
    "preview": {
      "distribution": "internal"
    },
    "production": {
      "autoIncrement": true
    }
  },
  "submit": {
    "production": {}
  }
}
